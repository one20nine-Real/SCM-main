import PageHeader from '@/components/shell/page-header'; import Panel from '@/components/ui/panel'; import { getModelPerformance } from '@/lib/scm'; import { setManualChampion } from '../actions';
export const dynamic = 'force-dynamic';
export default async function ChampionsPage() { const result = await getModelPerformance(); const rows = result.rows; return <><PageHeader eyebrow="ADMIN / FORECAST" title="Champion Models" description="수동 Champion 지정은 사유와 함께 이력으로 저장됩니다." /><Panel className="section" title="수동 지정">{result.error ? <p className="text-danger">조회에 실패했습니다: {result.error}</p> : rows.length ? <div className="analysis-table-wrap"><table className="analysis-table"><thead><tr><th>SKU</th><th>Backtest</th><th>Model</th><th>WAPE</th><th>사유</th><th>저장</th></tr></thead><tbody>{rows.map((row) => <tr key={`${row.backtestRunId}-${row.itemId}-${row.modelId}`}><td>{row.itemId}</td><td><code>{row.backtestRunId.slice(0,8)}</code></td><td>{row.modelId}</td><td>{row.wape === null ? '—' : `${(row.wape * 100).toFixed(1)}%`}</td><td><form action={setManualChampion}><input type="hidden" name="itemId" value={row.itemId} /><input type="hidden" name="modelId" value={row.modelId} /><input type="hidden" name="backtestRunId" value={row.backtestRunId} /><input name="reason" required placeholder="수동 지정 사유" /><button className="button" type="submit">지정</button></form></td><td /></tr>)}</tbody></table></div> : <p>Backtest 결과가 없습니다.</p>}</Panel></> }
