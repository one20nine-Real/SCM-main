import PageHeader from '@/components/shell/page-header'; import Panel from '@/components/ui/panel'; import Badge from '@/components/ui/badge'; import { getModelConfigs } from '@/lib/scm'; import { runBaselineForecast, toggleForecastModel } from './actions';
export const dynamic = 'force-dynamic';
export default async function ForecastModelsPage() { const result = await getModelConfigs(); return <><PageHeader eyebrow="ADMIN / FORECAST" title="Forecast Models" description="DB에 등록된 Baseline 모델과 활성화 상태를 관리합니다." /><Panel className="section" title="Model Registry" description="parameters와 applicable demand type은 DB 설정입니다.">{result.error ? <p className="text-danger">조회에 실패했습니다: {result.error}</p> : <div className="analysis-table-wrap"><table className="analysis-table"><thead><tr>{['모델명','family','engine','version','enabled','Demand Type','parameters','변경'].map((x) => <th key={x}>{x}</th>)}</tr></thead><tbody>{result.rows.length ? result.rows.map((row) => <tr key={row.modelId}><td>{row.modelName} ({row.modelId})</td><td>{row.family}</td><td>{row.engine}</td><td>{row.version}</td><td><Badge status={row.enabled ? 'SAFE' : 'WARNING'}>{row.enabled ? 'ON' : 'OFF'}</Badge></td><td>{row.applicableDemandType.join(', ')}</td><td><code>{JSON.stringify(row.parameters)}</code></td><td><form action={toggleForecastModel}><input type="hidden" name="modelId" value={row.modelId} /><input type="hidden" name="enabled" value={String(!row.enabled)} /><button className="button" type="submit">{row.enabled ? 'OFF' : 'ON'}</button></form></td></tr>) : <tr><td colSpan={8}>표시할 모델이 없습니다.</td></tr>}</tbody></table></div>}<form action={runBaselineForecast} className="button-row section"><button className="button primary" type="submit">Baseline Forecast 실행</button><a className="button" href="/admin/forecast-runs">실행 이력 보기</a></form></Panel></> }
