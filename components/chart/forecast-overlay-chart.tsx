import type { ForecastComparison } from '@/lib/scm-model';
export default function ForecastOverlayChart({ rows }: { rows: ForecastComparison[] }) { return <div className="chart-wrapper" aria-label="Actual과 Forecast 비교"><div className="chart-legend"><span>Actual</span><span>Forecast P50</span><span>Prediction Interval P80/P90</span></div><div className="analysis-table-wrap"><table className="analysis-table"><thead><tr><th>기간</th><th>Actual</th><th>P50</th><th>P80</th><th>P90</th></tr></thead><tbody>{rows.map((row) => <tr key={`${row.modelId}-${row.itemId}-${row.period}`}><td>{row.period.slice(0, 7)}</td><td>{row.actualQty ?? '—'}</td><td>{row.p50 ?? '—'}</td><td>{row.p80 ?? '—'}</td><td>{row.p90 ?? '—'}</td></tr>)}</tbody></table></div></div>; }
