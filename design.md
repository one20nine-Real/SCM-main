# superSCM 시스템 디자인 가이드

> 이 문서는 월간 발주계획 시스템의 화면을 일관된 시각 언어로 만들기 위한 기준 문서입니다. 새로운 화면이나 컴포넌트를 추가할 때 먼저 이 문서를 확인합니다.

## 1. 디자인 방향

superSCM은 해외 생산법인 12곳의 조달 흐름과 월간 발주 판단을 한 화면에서 빠르게 이해하도록 돕는 업무 시스템입니다. 디자인은 장식보다 **판단 속도, 정보 위계, 상태 구분**을 우선합니다.

핵심 인상은 다음과 같습니다.

- 후지필름을 연상시키는 선명한 레드 포인트
- 차갑지 않은 회백색 배경과 흰색 표면
- 작은 라운드와 얕은 그림자로 구성한 정돈된 업무 화면
- 숫자와 상태를 먼저 읽을 수 있는 명확한 위계
- 데스크톱의 좌측 업무 내비게이션과 모바일의 압축된 탐색 구조

참고 HTML의 모바일 대시보드 분위기와 색상 관계를 참고하되, 프로젝트의 기술 규칙에 맞게 순수 CSS와 기존 공통 클래스로 구현합니다. Tailwind CDN, styled-components, CSS Modules, 새 UI 프레임워크는 사용하지 않습니다.

## 2. 적용 우선순위

화면을 설계할 때 다음 순서로 판단합니다.

1. 업무 목적과 사용자가 내려야 할 판단을 먼저 배치합니다.
2. 위험·오류·확정 상태를 색상만이 아니라 문구와 배지로 함께 표시합니다.
3. 공통 토큰과 기존 클래스(`app-shell`, `card`, `grid`, `analysis-table` 등)를 우선 재사용합니다.
4. 화면별 예외 스타일은 최소화하고, 반복되면 `app/globals.css`의 공통 규칙으로 승격합니다.
5. 데스크톱에서 완성한 뒤 모바일에서 정보 손실 없이 재배치합니다.

## 3. 색상 시스템

색상은 의미를 전달하는 신호로 사용하며, 한 화면에서 강조 색상을 여러 개 경쟁시키지 않습니다.

### 3.1 기본 토큰

아래 토큰을 `app/globals.css`의 `:root`에 정의하고 화면에서는 토큰을 사용합니다. 기존 토큰과 의미가 겹치는 경우 아래 표의 의미 체계를 우선하되, 대규모 일괄 변경은 관련 화면을 함께 확인한 뒤 진행합니다.

| 역할 | 권장 값 | 용도 |
|---|---|---|
| `--ink` | `#1b1c1c` | 본문, 주요 숫자, 제목 |
| `--muted` | `#6d6b6b` | 보조 설명, 비활성 텍스트 |
| `--canvas` | `#fbf9f8` | 전체 배경 |
| `--surface` | `#ffffff` | 카드, 표, 입력 영역 |
| `--surface-container` | `#efeded` | 낮은 대비의 카드·요약 영역 |
| `--primary` | `#bb0014` | 주요 버튼, 활성 탭, 핵심 강조 |
| `--primary-strong` | `#93000d` | hover 또는 더 강한 강조 |
| `--green` | `#19734b` | 정상, 완료, 안전 |
| `--amber` | `#9a5b0d` | 주의, 검토 필요 |
| `--red` | `#ae3a37` | 위험, 오류, 긴급 |
| `--line` | `#e3e0df` | 테두리, 구분선 |

### 3.2 사용 규칙

- 레드는 브랜드 포인트이자 긴급도 신호입니다. 모든 버튼을 레드로 만들지 않습니다.
- `SAFE`·완료는 초록, 검토 필요는 앰버, `CRITICAL`·오류는 레드로 통일합니다.
- 상태 색상은 반드시 텍스트, 아이콘, 배지 중 최소 두 가지 방식으로 전달합니다.
- 본문 텍스트에는 순수한 검정을 사용하지 않고 `--ink`를 사용합니다.
- 배경과 표면의 명도 차이는 작게 유지하고, 경계는 그림자보다 선으로 먼저 구분합니다.
- 명도 대비가 낮은 회색 텍스트를 표 제목이나 오류 메시지에 사용하지 않습니다.

## 4. 타이포그래피

참고 HTML의 Hanken Grotesk·Inter·JetBrains Mono 조합을 방향으로 삼되, 한국어 가독성을 위해 기본 본문은 `Noto Sans KR`을 사용합니다.

| 계층 | 크기 / 굵기 | 용도 |
|---|---:|---|
| 화면 제목 | 25–28px / 800 | 페이지의 핵심 제목 |
| 섹션 제목 | 15–18px / 700 | 카드·섹션 제목 |
| 주요 수치 | 27–34px / 800 | KPI, 발주량, 위험 건수 |
| 본문 | 13–14px / 400–500 | 설명, 표 본문 |
| 보조 문구 | 11–12px / 400–700 | 단위, 기준일, 도움말 |
| 영문 kicker | 10–11px / 700–800 | `ANALYSIS`, `WORKFLOW` 등 구획 표시 |
| 숫자·코드 | JetBrains Mono 계열 | 품목 코드, 날짜, 수량, 리드타임 |

규칙:

- 제목은 짧게 쓰고, 설명은 제목 아래 한 문장으로 제공합니다.
- 숫자는 오른쪽 정렬하고 단위는 숫자와 시각적으로 분리합니다.
- 품목 코드·공급처 코드·날짜는 고정폭 글꼴을 사용해 열 정렬을 돕습니다.
- 영문 kicker는 모두 대문자, 넓은 자간으로 사용하되 본문 정보를 대신하지 않습니다.
- 한국어 문장은 `word-break: keep-all`을 유지합니다.

## 5. 간격·모서리·그림자

기본 간격 단위는 4px입니다.

| 토큰 | 값 | 대표 용도 |
|---|---:|---|
| `base` | 4px | 아이콘과 텍스트 사이 |
| `stack-sm` | 8px | 인접한 라벨·값 |
| `stack-md` | 16px | 카드 내부 기본 여백 |
| `stack-lg` | 24px | 섹션 간격 |
| `gutter-md` | 24px | 화면 좌우 여백 |
| `margin-lg` | 32px | 데스크톱 콘텐츠 여백 |

- 카드 기본 반경은 12–14px입니다.
- 버튼과 입력은 7–9px, 상태 배지는 pill 형태를 사용합니다.
- 그림자는 `0 3px 12px rgba(16, 43, 75, 0.04)` 수준의 얕은 그림자를 기본으로 합니다.
- 떠 있는 헤더·내비게이션은 backdrop blur를 사용할 수 있지만, 콘텐츠 가독성을 해치지 않도록 불투명도를 높게 유지합니다.
- 둥근 모서리와 그림자를 동시에 과도하게 사용하지 않습니다.

## 6. 레이아웃 원칙

### 6.1 데스크톱

- 전체 구조는 `.app-shell` 안에 `.sidebar`와 `.main`을 둡니다.
- 사이드바는 업무 플로우와 분석 메뉴를 분리합니다.
- 상단 바에는 현재 화면명, 기준월, 연결 상태를 둡니다.
- 본문 최대 너비는 1280–1440px 범위로 제한하고, 넓은 화면에서는 좌우 여백을 확보합니다.
- KPI는 `.grid-4` 또는 `.grid-3`, 비교 섹션은 `.grid-2`를 우선 사용합니다.

### 6.2 모바일

- 760px 이하에서 사이드바는 상단 영역으로 축소하고 내비게이션은 3열 이하로 재배치합니다.
- 카드 그리드는 1열로 바꾸며, KPI는 라벨·값·상태가 한 번에 보이게 합니다.
- 넓은 표는 억지로 줄이지 말고 `.analysis-table-wrap`의 가로 스크롤을 허용합니다.
- 긴 진행 단계는 가로 스크롤 가능한 영역으로 감싸고, 현재 단계가 보이는 위치를 우선합니다.
- 모바일에서 hover에 의존하는 정보는 항상 표시하거나 버튼·탭·상세 화면으로 대체합니다.

## 7. 공통 컴포넌트 규칙

### 카드와 KPI

- `.card`는 하나의 질문에 답해야 합니다.
- `.metric`은 라벨 → 큰 숫자 → 단위·변화·기준 순서로 읽힙니다.
- KPI의 강조도는 배경색보다 숫자 크기와 상태 문구로 조절합니다.
- 클릭 가능한 카드는 `metric-interactive`를 사용하고 키보드 포커스 스타일을 제공합니다.

### 버튼

- 가장 중요한 한 가지 행동만 `.button.primary`로 표시합니다.
- 보조 행동은 기본 `.button`, 가벼운 보조 탐색은 `.button.ghost`를 사용합니다.
- 버튼 문구는 동사로 끝냅니다. 예: `발주량 계산`, `보고서 확인`, `다시 조회`.
- 비활성 버튼은 opacity만 낮추지 말고 비활성 이유를 주변 설명으로 제공합니다.

### 배지와 상태

- `.tag.green`, `.tag.amber`, `.tag.red`, `.tag.gray`를 상태 표현의 기본으로 사용합니다.
- `SUPABASE LIVE`, `LOCAL PROTOTYPE`처럼 연결 상태는 `.local-badge`를 사용합니다.
- 상태명은 영어 원문을 그대로 노출할 수 있지만, 사용자에게 의미를 전달하는 한국어 설명을 함께 둡니다.
- `UNKNOWN`은 위험으로 오인되지 않도록 회색 배지와 원인 문구를 함께 표시합니다.

### 탭·진행 단계

- 탭은 현재 위치를 레드와 하단 선으로 표시합니다.
- 진행 단계는 완료·현재·예정의 세 상태를 구분합니다.
- 단계 이동이 데이터 확정이나 저장을 의미한다면, 단순한 색상 변화만으로 완료 처리하지 않습니다.

### 표

- 표 헤더는 옅은 표면색과 굵은 글씨로 구분합니다.
- 숫자 열은 오른쪽 정렬, 코드 열은 고정폭 글꼴을 사용합니다.
- 행 hover는 보조 피드백이며, 의미 있는 상태는 hover 없이도 보여야 합니다.
- 긴 표는 페이지 전체를 밀어내지 않고 표 컨테이너 안에서 스크롤합니다.
- 빈 결과와 조회 오류를 같은 시각 상태로 표현하지 않습니다.

## 8. 데이터 상태 표현

화면 디자인은 데이터의 의미를 바꾸지 않습니다. 특히 계산 불가 값을 임의의 숫자로 채우지 않습니다.

| 데이터 상태 | 화면 표현 |
|---|---|
| 정상 값 | 값 + 단위 + 기준일 또는 산출 기준 |
| `NO_USAGE` | `계산 불가` + `사용 이력 없음` |
| `NO_LEADTIME` | `계산 불가` + `리드타임 미확정` |
| `UNKNOWN` | 회색 배지 + `판정 근거 부족` |
| 조회 오류 | 오류 문구 + 재시도 방법 |
| 빈 결과 | `표시할 데이터가 없습니다` |
| 저장 중 | 버튼 비활성 + 진행 상태 |
| 저장 완료 | 완료 배지 + 저장 시각 |

숫자 계산은 SQL 또는 `lib/scm-model.ts`의 순수 모델 함수에서 담당하며, 화면 컴포넌트는 결과와 사유를 표시하는 역할만 합니다.

## 9. 아이콘·모션·이미지

- 기존 프로젝트의 `lucide-react` 아이콘을 우선 사용합니다.
- 아이콘은 장식보다 의미 전달을 위해 사용하고, 아이콘만으로 중요한 상태를 전달하지 않습니다.
- 기본 전환 시간은 150–200ms입니다. 큰 패널이나 진행 단계의 등장 애니메이션은 300–600ms 이내로 제한합니다.
- 차트·막대·상태 변화 애니메이션은 데이터가 나타나는 순서를 이해하는 데 도움이 될 때만 사용합니다.
- `prefers-reduced-motion: reduce` 환경에서는 transform과 transition을 줄입니다.
- 외부 이미지 URL에 의존하는 브랜드 로고는 피하고, 프로젝트에서 관리하는 자산이나 텍스트 마크를 사용합니다.

## 10. 접근성 기준

- 모든 버튼과 링크는 키보드로 접근할 수 있어야 합니다.
- `:focus-visible` 상태는 배경색 변화만이 아니라 명확한 외곽선 또는 링으로 표시합니다.
- 상태를 색상만으로 구분하지 않습니다.
- 표에는 적절한 헤더 구조와 읽을 수 있는 열 제목을 제공합니다.
- 아이콘 버튼에는 `aria-label`을 붙입니다.
- 모바일 터치 대상은 최소 40–44px의 클릭 영역을 확보합니다.
- 본문·표·상태 배지는 일반적인 대비 기준을 만족하도록 유지합니다.

## 11. 화면 제작 체크리스트

새 화면을 만들기 전에 다음을 확인합니다.

- [ ] 화면의 핵심 질문을 한 문장으로 설명할 수 있는가?
- [ ] 제목, 설명, 기준월 또는 조회 기준이 보이는가?
- [ ] 기존 공통 클래스와 토큰을 우선 재사용했는가?
- [ ] KPI·표·배지의 숫자와 상태가 같은 의미 체계를 사용하는가?
- [ ] 오류와 빈 결과를 구분했는가?
- [ ] 계산 불가 값을 임의 숫자로 표시하지 않았는가?
- [ ] 모바일에서 표와 진행 단계가 읽히는가?
- [ ] 키보드 포커스와 아이콘 대체 텍스트가 있는가?
- [ ] 색상 없이도 정상·주의·위험을 구분할 수 있는가?
- [ ] 화면 문구와 주석은 한국어로 작성했는가?
- [ ] 변경 후 `npm run build`를 실행했는가?

## 12. 구현 메모

- 전역 토큰과 공통 컴포넌트 스타일은 `app/globals.css`에서 관리합니다.
- 분석 화면은 `components/analysis/*`의 껍데기와 표 구조를 우선 재사용합니다.
- 새 분석 화면의 데이터 조회는 `lib/scm.ts`, 타입과 정규화는 `lib/scm-model.ts`에 둡니다.
- 화면 컴포넌트에서 Supabase를 직접 조회하지 않습니다.
- 참고 HTML의 Tailwind 클래스나 CDN을 프로젝트에 복사하지 않습니다.
- 기존 `components/workflow/`의 6개 스텝 파일은 디자인 토큰만으로 개선할 수 있을 때 우선 파일을 직접 수정하지 않습니다.

이 문서와 `AGENTS.md`, `SCHEMA.md`가 충돌하면 프로젝트의 데이터·기술 규칙을 우선하고, 디자인 표현만 이 문서의 기준을 따릅니다.
